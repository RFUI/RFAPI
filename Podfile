
target 'Example-iOS' do
    platform :ios, '8.0'
    pod 'RFMessageManager', :subspecs => ['SVProgressHUD']
    pod 'RFAPI', :path => '.'

    target 'Test-iOS' do
    end
end

target 'Test-macOS' do
    platform :osx, '10.9'
    pod 'RFAPI', :path => '.'
end
